---
phase: 02-worker-resilience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/worker/logging_config.py
  - apps/worker/errors.py
  - apps/worker/requirements.txt
  - apps/worker/pipeline/models.py
  - apps/worker/pyproject.toml
  - apps/worker/tests/__init__.py
  - apps/worker/tests/conftest.py
  - apps/worker/tests/test_error_classification.py
  - apps/worker/tests/test_shutdown.py
  - apps/worker/tests/test_logging.py
  - apps/worker/tests/test_reconnection.py
  - apps/worker/tests/test_progress.py
  - apps/gateway/migrations/002_worker_progress.up.sql
  - apps/gateway/migrations/002_worker_progress.down.sql
autonomous: true
requirements:
  - WRK-03
  - WRK-04
  - WRK-05
  - WRK-06

must_haves:
  truths:
    - "structlog is configured and produces JSON output"
    - "LLM error hierarchy exists with Auth, RateLimit, ContentPolicy, and Transient subclasses"
    - "DB migration adds pipeline_phase, translated_count, total_count, error_detail columns to documents"
    - "pytest runs with test scaffolds covering all WRK requirements"
    - "SQLAlchemy Document model mirrors new DB columns"
  artifacts:
    - path: "apps/worker/logging_config.py"
      provides: "structlog JSON configuration"
      contains: "structlog.configure"
    - path: "apps/worker/errors.py"
      provides: "LLM exception hierarchy"
      exports: ["LLMError", "LLMAuthError", "LLMRateLimitError", "LLMContentPolicyError", "LLMTransientError"]
    - path: "apps/gateway/migrations/002_worker_progress.up.sql"
      provides: "DB schema for progress tracking"
      contains: "pipeline_phase"
    - path: "apps/worker/tests/conftest.py"
      provides: "Shared test fixtures"
      contains: "mock_db"
    - path: "apps/worker/pyproject.toml"
      provides: "pytest configuration"
      contains: "[tool.pytest"
  key_links:
    - from: "apps/worker/pipeline/models.py"
      to: "apps/gateway/migrations/002_worker_progress.up.sql"
      via: "SQLAlchemy model mirrors migration columns"
      pattern: "pipeline_phase.*total_count.*translated_count.*error_detail"
---

<objective>
Create the foundation layer for Phase 2: structured logging config, LLM error hierarchy, database migration for progress fields, pytest infrastructure with test scaffolds, and updated SQLAlchemy models.

Purpose: Every subsequent plan depends on these shared definitions -- structlog config, error types, DB columns, and test infrastructure. Building them first prevents circular dependencies.
Output: logging_config.py, errors.py, migration files, pytest setup, updated models.py, test scaffolds for all WRK requirements.
</objective>

<execution_context>
@/Users/haoyuliu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haoyuliu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-worker-resilience/02-CONTEXT.md
@.planning/phases/02-worker-resilience/02-RESEARCH.md
@.planning/phases/02-worker-resilience/02-VALIDATION.md

<interfaces>
<!-- Current state of files this plan touches -->

From apps/worker/pipeline/models.py (Document model -- needs new columns):
```python
class Document(Base):
    __tablename__ = 'documents'
    id = Column(String, primary_key=True)
    user_id = Column(String, nullable=True)
    original_name = Column(String, nullable=True)
    s3_key = Column(String, nullable=True)
    status = Column(String, nullable=True)
    target_lang = Column(String, nullable=True)
    result = Column(JSONB, nullable=True)
    created_at = Column(DateTime)
```

From apps/worker/requirements.txt (current deps):
```
redis
psycopg2-binary
requests
pymupdf
boto3
google-genai
anthropic
openai
pydantic
tenacity
SQLAlchemy
cryptography
```

From apps/gateway/migrations/001_initial_schema.up.sql (documents table):
```sql
CREATE TABLE IF NOT EXISTS documents (
    id            TEXT PRIMARY KEY,
    user_id       TEXT REFERENCES users(id) ON DELETE CASCADE,
    original_name TEXT,
    s3_key        TEXT,
    status        TEXT,
    target_lang   TEXT,
    result        JSONB,
    llm_provider  TEXT,
    llm_model     TEXT,
    created_at    TIMESTAMPTZ
);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create structlog config, LLM error hierarchy, and DB migration</name>
  <files>
    apps/worker/logging_config.py,
    apps/worker/errors.py,
    apps/gateway/migrations/002_worker_progress.up.sql,
    apps/gateway/migrations/002_worker_progress.down.sql,
    apps/worker/pipeline/models.py,
    apps/worker/requirements.txt
  </files>
  <read_first>
    apps/worker/pipeline/models.py,
    apps/worker/requirements.txt,
    apps/gateway/migrations/001_initial_schema.up.sql,
    apps/worker/pipeline/llm_client.py,
    apps/worker/config.py
  </read_first>
  <action>
1. Create `apps/worker/logging_config.py`:
   - Import structlog, logging, sys
   - Function `configure_logging(dev_mode: bool = False)` that:
     - Defines shared_processors: `merge_contextvars`, `add_log_level`, `add_logger_name`, `TimeStamper(fmt="iso")`, `StackInfoRenderer()`, `format_exc_info`
     - Uses `structlog.dev.ConsoleRenderer()` when dev_mode=True, `structlog.processors.JSONRenderer()` when False
     - Calls `structlog.configure()` with `logger_factory=structlog.stdlib.LoggerFactory()`, `wrapper_class=structlog.stdlib.BoundLogger`, `cache_logger_on_first_use=True`
     - Sets up a `logging.StreamHandler(sys.stdout)` with `structlog.stdlib.ProcessorFormatter`
     - Attaches handler to root logger at `logging.INFO` level
   - dev_mode defaults to checking `os.getenv("DEV_MODE", "false").lower() == "true"`

2. Create `apps/worker/errors.py`:
   - `class LLMError(Exception)` with `__init__(self, message, provider, original_error=None)` storing all three as attributes
   - `class LLMAuthError(LLMError)` -- docstring: "401/403 - Invalid or expired API key. Fatal, no retry."
   - `class LLMRateLimitError(LLMError)` -- docstring: "429 - Rate limit exceeded. Transient, retry with backoff."
   - `class LLMContentPolicyError(LLMError)` -- docstring: "Content refused by model safety filters. Skip chunk, continue."
   - `class LLMTransientError(LLMError)` -- docstring: "5xx, timeout, connection error. Transient, retry with backoff."

3. Create `apps/gateway/migrations/002_worker_progress.up.sql`:
   ```sql
   ALTER TABLE documents ADD COLUMN IF NOT EXISTS pipeline_phase TEXT;
   ALTER TABLE documents ADD COLUMN IF NOT EXISTS translated_count INTEGER DEFAULT 0;
   ALTER TABLE documents ADD COLUMN IF NOT EXISTS total_count INTEGER DEFAULT 0;
   ALTER TABLE documents ADD COLUMN IF NOT EXISTS error_detail JSONB;
   ```

4. Create `apps/gateway/migrations/002_worker_progress.down.sql`:
   ```sql
   ALTER TABLE documents DROP COLUMN IF EXISTS pipeline_phase;
   ALTER TABLE documents DROP COLUMN IF EXISTS translated_count;
   ALTER TABLE documents DROP COLUMN IF EXISTS total_count;
   ALTER TABLE documents DROP COLUMN IF EXISTS error_detail;
   ```

5. Update `apps/worker/pipeline/models.py` -- add to the Document class:
   - `pipeline_phase = Column(String, nullable=True)`
   - `translated_count = Column(Integer, default=0)`
   - `total_count = Column(Integer, default=0)`
   - `error_detail = Column(JSONB, nullable=True)`
   Import Integer if not already imported (it is).

6. Update `apps/worker/requirements.txt` -- add these lines:
   ```
   structlog
   pytest
   pytest-timeout
   ```
  </action>
  <verify>
    <automated>cd /Users/haoyuliu/BilingualReader && python -c "from apps.worker.logging_config import configure_logging; configure_logging()" && python -c "from apps.worker.errors import LLMError, LLMAuthError, LLMRateLimitError, LLMContentPolicyError, LLMTransientError; e = LLMAuthError('test', 'openai'); assert e.provider == 'openai'" && cat apps/gateway/migrations/002_worker_progress.up.sql | grep pipeline_phase && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - apps/worker/logging_config.py contains `def configure_logging(`
    - apps/worker/logging_config.py contains `structlog.configure(`
    - apps/worker/logging_config.py contains `JSONRenderer`
    - apps/worker/errors.py contains `class LLMError(Exception):`
    - apps/worker/errors.py contains `class LLMAuthError(LLMError):`
    - apps/worker/errors.py contains `class LLMRateLimitError(LLMError):`
    - apps/worker/errors.py contains `class LLMContentPolicyError(LLMError):`
    - apps/worker/errors.py contains `class LLMTransientError(LLMError):`
    - apps/worker/errors.py contains `self.provider = provider`
    - apps/gateway/migrations/002_worker_progress.up.sql contains `pipeline_phase TEXT`
    - apps/gateway/migrations/002_worker_progress.up.sql contains `translated_count INTEGER`
    - apps/gateway/migrations/002_worker_progress.up.sql contains `total_count INTEGER`
    - apps/gateway/migrations/002_worker_progress.up.sql contains `error_detail JSONB`
    - apps/gateway/migrations/002_worker_progress.down.sql contains `DROP COLUMN IF EXISTS pipeline_phase`
    - apps/worker/pipeline/models.py contains `pipeline_phase = Column(`
    - apps/worker/pipeline/models.py contains `translated_count = Column(`
    - apps/worker/pipeline/models.py contains `total_count = Column(`
    - apps/worker/pipeline/models.py contains `error_detail = Column(`
    - apps/worker/requirements.txt contains `structlog`
    - apps/worker/requirements.txt contains `pytest`
  </acceptance_criteria>
  <done>structlog config module exists and is importable. Error hierarchy has 5 classes (LLMError + 4 subclasses) with provider attribute. Migration 002 adds 4 columns to documents table. SQLAlchemy Document model has matching columns. requirements.txt includes structlog, pytest, pytest-timeout.</done>
</task>

<task type="auto">
  <name>Task 2: Create pytest infrastructure and test scaffolds for all WRK requirements</name>
  <files>
    apps/worker/pyproject.toml,
    apps/worker/tests/__init__.py,
    apps/worker/tests/conftest.py,
    apps/worker/tests/test_error_classification.py,
    apps/worker/tests/test_shutdown.py,
    apps/worker/tests/test_logging.py,
    apps/worker/tests/test_reconnection.py,
    apps/worker/tests/test_progress.py
  </files>
  <read_first>
    apps/worker/errors.py,
    apps/worker/logging_config.py,
    apps/worker/services/db.py,
    apps/worker/services/queue.py,
    apps/worker/main.py,
    apps/worker/pipeline/llm_client.py,
    apps/worker/pipeline/translator.py
  </read_first>
  <action>
1. Create `apps/worker/pyproject.toml`:
   ```toml
   [tool.pytest.ini_options]
   testpaths = ["tests"]
   timeout = 30
   python_files = ["test_*.py"]
   python_classes = ["Test*"]
   python_functions = ["test_*"]
   ```

2. Create `apps/worker/tests/__init__.py` (empty file).

3. Create `apps/worker/tests/conftest.py` with shared fixtures:
   - `import pytest, sys, os` then `sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))` to make apps/worker importable
   - Fixture `mock_db_service` that creates a MagicMock of DBService with `update_job_status`, `update_progress`, `update_error` methods
   - Fixture `mock_queue_service` that creates a MagicMock of QueueService with `get_task`, `push_task`, `client` attributes
   - Fixture `mock_llm_client` that creates a MagicMock of LLMClient with `generate` method and `config` attribute (config.provider="openai")
   - Import from `unittest.mock import MagicMock, patch`

4. Create `apps/worker/tests/test_shutdown.py` (WRK-01):
   - `test_shutdown_flag_set_on_sigterm`: Import signal, threading; create `threading.Event()` as shutdown_requested; define handler that calls `shutdown_requested.set()`; register via `signal.signal(signal.SIGTERM, handler)`; send `os.kill(os.getpid(), signal.SIGTERM)`; assert `shutdown_requested.is_set()` is True. Restore original handler after.
   - `test_main_loop_exits_on_shutdown_flag`: Placeholder that asserts True (will be filled in Plan 02 when main.py is refactored). Mark with `@pytest.mark.skip(reason="Implemented in plan 02")`
   - `test_interrupted_status_set_on_shutdown`: Placeholder skip. Mark with `@pytest.mark.skip(reason="Implemented in plan 02")`

5. Create `apps/worker/tests/test_reconnection.py` (WRK-02):
   - `test_redis_client_has_retry_config`: Placeholder skip. Mark with `@pytest.mark.skip(reason="Implemented in plan 02")`
   - `test_db_service_reconnects_on_failure`: Placeholder skip. Mark with `@pytest.mark.skip(reason="Implemented in plan 02")`

6. Create `apps/worker/tests/test_logging.py` (WRK-03):
   - `test_structlog_configured_json_output`: Import `configure_logging` from `logging_config`, call `configure_logging(dev_mode=False)`. Get a structlog logger via `structlog.get_logger()`. Use `structlog.testing.capture_logs()` context manager, log `log.info("test.event", key="value")`. Assert the captured log entry contains `event="test.event"` and `key="value"`.
   - `test_no_print_statements_in_source`: Use `subprocess.run(["grep", "-rn", "print(", "apps/worker/"], capture_output=True, text=True)`. This is a placeholder -- mark skip with reason "Validated after plan 02 replaces all print() calls".

7. Create `apps/worker/tests/test_error_classification.py` (WRK-06):
   - `test_llm_auth_error_is_llm_error`: `from errors import LLMAuthError, LLMError; e = LLMAuthError("bad key", "openai"); assert isinstance(e, LLMError); assert e.provider == "openai"`
   - `test_llm_rate_limit_error_is_llm_error`: Same pattern for LLMRateLimitError
   - `test_llm_content_policy_error_is_llm_error`: Same pattern for LLMContentPolicyError
   - `test_llm_transient_error_is_llm_error`: Same pattern for LLMTransientError
   - `test_llm_error_stores_original_error`: `orig = ValueError("orig"); e = LLMAuthError("msg", "gemini", orig); assert e.original_error is orig`
   - `test_auth_error_not_retried`: Placeholder skip. Mark with `@pytest.mark.skip(reason="Implemented in plan 03")`
   - `test_rate_limit_error_retried_with_backoff`: Placeholder skip.

8. Create `apps/worker/tests/test_progress.py` (WRK-04, WRK-05):
   - `test_document_model_has_progress_fields`: `from pipeline.models import Document; assert hasattr(Document, 'pipeline_phase'); assert hasattr(Document, 'translated_count'); assert hasattr(Document, 'total_count'); assert hasattr(Document, 'error_detail')`
   - `test_progress_update_method`: Placeholder skip. Mark with `@pytest.mark.skip(reason="Implemented in plan 03")`
   - `test_error_detail_stored_as_json`: Placeholder skip.
  </action>
  <verify>
    <automated>cd /Users/haoyuliu/BilingualReader/apps/worker && pip install structlog pytest pytest-timeout -q && python -m pytest tests/ -x -q --timeout=30 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - apps/worker/pyproject.toml contains `[tool.pytest.ini_options]`
    - apps/worker/pyproject.toml contains `testpaths = ["tests"]`
    - apps/worker/tests/__init__.py exists
    - apps/worker/tests/conftest.py contains `mock_db_service`
    - apps/worker/tests/conftest.py contains `mock_queue_service`
    - apps/worker/tests/conftest.py contains `mock_llm_client`
    - apps/worker/tests/test_shutdown.py contains `test_shutdown_flag_set_on_sigterm`
    - apps/worker/tests/test_reconnection.py contains `test_redis_client_has_retry_config`
    - apps/worker/tests/test_logging.py contains `test_structlog_configured_json_output`
    - apps/worker/tests/test_error_classification.py contains `test_llm_auth_error_is_llm_error`
    - apps/worker/tests/test_error_classification.py contains `test_llm_error_stores_original_error`
    - apps/worker/tests/test_progress.py contains `test_document_model_has_progress_fields`
    - `python -m pytest tests/ -x -q` exits 0 (all non-skipped tests pass, skipped tests are marked)
  </acceptance_criteria>
  <done>pytest runs successfully from apps/worker/. Non-skipped tests pass (error hierarchy tests, structlog config test, Document model fields test, signal flag test). Skipped tests are marked with reasons pointing to future plans. conftest.py provides mock fixtures for DB, Queue, and LLM client.</done>
</task>

</tasks>

<verification>
- `cd apps/worker && python -m pytest tests/ -v --timeout=30` -- all non-skipped tests pass
- `python -c "from apps.worker.errors import LLMAuthError; print('OK')"` -- imports successfully
- `python -c "from apps.worker.logging_config import configure_logging; configure_logging(); import structlog; structlog.get_logger().info('test')"` -- no errors
- `cat apps/gateway/migrations/002_worker_progress.up.sql` -- contains 4 ALTER TABLE statements
- `python -c "from apps.worker.pipeline.models import Document; assert hasattr(Document, 'pipeline_phase')"` -- passes
</verification>

<success_criteria>
- structlog configuration module exists and produces JSON output when called
- Error hierarchy has 5 classes with correct inheritance and provider attribute
- Migration 002 adds pipeline_phase, translated_count, total_count, error_detail to documents
- Document SQLAlchemy model has matching 4 new columns
- pytest infrastructure works with all test files importable and runnable
- requirements.txt includes structlog, pytest, pytest-timeout
</success_criteria>

<output>
After completion, create `.planning/phases/02-worker-resilience/02-01-SUMMARY.md`
</output>
